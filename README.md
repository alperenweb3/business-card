# alperen.co

Alperen Özkan’s bilingual personal website. English is the default language;
visitors can switch to Turkish from the header.

## Development

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.
